# Browser-safe application URL only. Do not place provider OAuth tokens here.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase application database connection values are configured during deployment.
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# OAuth client identifiers/secrets are server-side and should be provisioned through
# the deployment platform secret manager, not committed to this repository.
