# Project Setup Bundle

This directory contains generated setup and launch guidance tied to a canonical project revision. Verify external instructions against the compatibility registry before executing them.
