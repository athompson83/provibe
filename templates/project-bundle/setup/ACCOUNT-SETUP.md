# Account Setup

For each required service: purpose, owner, account/project naming, environment separation, permissions, billing implications, callback/domain requirements, verification and credentials that must never be committed.
