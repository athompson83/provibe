# Responsive Behavior

For each major screen/component define desktop/tablet/mobile layout changes, priority/order, overflow strategy, touch targets, navigation transformation and content that must never be hidden.
