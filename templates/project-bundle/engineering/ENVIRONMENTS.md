# Environments

Define local/development/preview/staging/production boundaries, domains, databases, provider accounts, environment-key names, deployment promotion, test data and rules preventing production secrets/data from leaking downward.
