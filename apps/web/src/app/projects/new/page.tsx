import Link from 'next/link';
import { createProjectAction } from './actions';

export default async function NewProjectPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const params = await searchParams;
  return <main style={{minHeight:'100vh',background:'#f5f7f8',padding:'48px 24px'}}><section style={{maxWidth:640,margin:'0 auto',background:'white',border:'1px solid #e3e8eb',borderRadius:20,padding:32}}><Link href="/projects" style={{color:'#667085',fontSize:14}}>← Portfolio</Link><h1 style={{fontSize:38,letterSpacing:'-.04em',margin:'26px 0 8px'}}>Start a project</h1><p style={{color:'#667085',lineHeight:1.6}}>Create the canonical workspace first. Blueprint answers, evidence, integrations, and generated artifacts will attach to this project.</p>{params.error ? <p style={{background:'#fff0ef',color:'#9e2924',padding:12,borderRadius:10}}>{params.error}</p> : null}<form action={createProjectAction} style={{display:'grid',gap:18,marginTop:28}}><label style={{display:'grid',gap:7,fontWeight:650,fontSize:13}}>Workspace name<input name="workspaceName" required placeholder="Acme Studio" style={{padding:13,border:'1px solid #d4dbdf',borderRadius:12,font:'inherit'}} /></label><label style={{display:'grid',gap:7,fontWeight:650,fontSize:13}}>Project name<input name="projectName" required placeholder="Customer Portal" style={{padding:13,border:'1px solid #d4dbdf',borderRadius:12,font:'inherit'}} /></label><button type="submit" style={{padding:14,border:0,borderRadius:12,background:'#111827',color:'white',fontWeight:700}}>Create project</button></form></section></main>;
}
