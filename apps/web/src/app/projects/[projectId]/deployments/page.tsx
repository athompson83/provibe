import { deployments } from '../../../../lib/demo-data';
import { StatusBadge } from '../../../../components/status-badge';
export default function DeploymentsPage(){return <><section className="page-heading"><div><span className="eyebrow">Deployments</span><h1>Know exactly what version is live.</h1><p>Translate provider state into application impact and reconcile deployment SHAs against repository state.</p></div></section><section className="deployment-hero"><div><span className="eyebrow">Production</span><h2>Aligned with GitHub main</h2><p>Commit <code>a83f9c1</code> is currently live and the latest GitHub checks passed.</p></div><StatusBadge state="verified"/></section><section className="data-table">{deployments.map(d=><div className="data-row deployment-row" key={`${d.environment}-${d.sha}`}><strong>{d.environment}</strong><code>{d.sha}</code><span>{d.source}</span><span>{d.age}</span><span className={d.status==='Error'?'text-danger':'text-success'}>{d.status}</span></div>)}</section></>;}
