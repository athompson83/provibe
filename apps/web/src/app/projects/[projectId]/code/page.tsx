import { CodeInspector } from '../../../../components/code-inspector';
import { folders } from '../../../../lib/demo-data';
export default function CodePage(){return <><section className="page-heading"><div><span className="eyebrow">Code explorer</span><h1>Read the application in product language.</h1><p>Start with purpose and impact. Drill into syntax only when you need it.</p></div></section><section className="folder-table"><div className="folder-row folder-head"><span>Folder</span><span>What it does</span><span>Touches</span><span>Change risk</span></div>{folders.map(folder=><div className="folder-row" key={folder.path}><code>{folder.path}</code><span>{folder.purpose}</span><span>{folder.touches}</span><strong>{folder.risk}</strong></div>)}</section><section className="section-block"><div className="section-title"><div><span className="eyebrow">Contextual explanation</span><h2>Select a line to understand it</h2></div></div><CodeInspector/></section></>;}
