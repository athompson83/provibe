import Link from 'next/link';
import { StatusBadge } from '../../../components/status-badge';
import { attention, project, providers, recentActivity } from '../../../lib/demo-data';
export default function OverviewPage() { return <><section className="page-heading"><div><span className="eyebrow">Build cockpit</span><h1>Know what is true before deciding what is next.</h1><p>{project.summary}</p></div><Link className="button button-primary" href="/projects/demo/ask">Ask my app</Link></section><section className="verdict-grid"><div className="verdict-card"><span className="eyebrow">Current verdict</span><h2>{project.verdict}</h2><p>One blocking QA gap and one observed database warning remain.</p><div className="health-meter"><span style={{width:`${project.health}%`}}/></div><small>{project.health}% launch readiness · evidence-weighted</small></div><div className="alignment-card"><span className="eyebrow">Production alignment</span><strong>main = production</strong><code>{project.productionSha}</code><p>GitHub and Vercel point to the same commit.</p><StatusBadge state="verified"/></div></section><section className="section-block"><div className="section-title"><div><span className="eyebrow">Requires attention</span><h2>Three things worth knowing now</h2></div></div><div className="attention-list">{attention.map(item=><article key={item.title}><StatusBadge state={item.state}/><div><h3>{item.title}</h3><p>{item.detail}</p></div><span className="row-arrow">→</span></article>)}</div></section><section className="two-column"><div className="section-block flush"><div className="section-title"><div><span className="eyebrow">Connections</span><h2>Source systems</h2></div></div><div className="provider-list">{providers.map(provider=><div key={provider.name}><span className="provider-logo">{provider.name[0]}</span><p><strong>{provider.name}</strong><small>{provider.detail}</small></p><span><b>{provider.state}</b><small>{provider.freshness}</small></span></div>)}</div></div><div className="section-block flush"><div className="section-title"><div><span className="eyebrow">Evidence timeline</span><h2>Recent verified activity</h2></div></div><div className="timeline">{recentActivity.map(item=><div key={item.title}><span>{item.when}</span><p><strong>{item.title}</strong><small>{item.source}</small></p><StatusBadge state={item.state}/></div>)}</div></div></section></>; }
