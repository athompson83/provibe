# ProVibe foundation adoption — 2026-09-10

Implementation base: `3855d5dc6bea00c131112435fd3c7fa1253617f4` on `agent/provibe-foundation`, as identified by main's README. This draft targets the implementation branch; it does not merge that foundation into main.
Read main README, implementation AGENTS/CLAUDE/manifest, and branch metadata. Original AGENTS preserved byte-for-byte as blob `83f9b7e7f2dc01bec0d5d119cca45d2d2150c875` in the operating reference. Claude-only entry content was duplicate shared guidance and is consolidated into the import.

Preserved structured canonical state/generated Markdown, evidence-first verdicts, read-only MVP, no customer-code execution, separate analyzer runtime, tenant isolation, versioned JSON prompts, and scope-stop decisions. Actual verify command is narrower than full lint/build/DB/E2E acceptance and is labelled accordingly.

No code, dependencies, workflows, schema, canonical data, existing product status, provider access, secrets, platform controls, or release changed. Runtime/fresh-agent/failure/CI-routing/hosted checks NOT_RUN; local cloning unavailable due to DNS resolution. Policy-only adoption, not live provider integration or foundation certification.
